/// <reference types="react" />
import { StyleProp } from "react-native";
import { AVPlaybackSource } from "expo-av";
interface Props {
    source: AVPlaybackSource;
    style?: StyleProp<any>;
    sliderColor?: string;
    completedTrackColor?: string;
    remainingTrackColor?: string;
    playSize?: number;
    playColor?: string;
}
export default function AudioPlayer({ source, style, sliderColor, completedTrackColor, remainingTrackColor, playSize, playColor, }: Props): JSX.Element;
export {};
//# sourceMappingURL=AudioPlayer.d.ts.map