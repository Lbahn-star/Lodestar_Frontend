'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var types = require('./types.js');

function getExtensionFromMimeType(mimeType) {
  switch (mimeType) {
    case types.MimeType.Csv:
      return '.csv';

    case types.MimeType.Gif:
      return '.gif';

    case types.MimeType.Glb:
      return '.glb';

    case types.MimeType.Html:
      return '.html';

    case types.MimeType.Jpeg:
      return '.jpg';

    case types.MimeType.Js:
      return '.js';

    case types.MimeType.Json:
      return '.json';

    case types.MimeType.Hls:
      return '.m3u8';

    case types.MimeType.Mov:
      return '.mov';

    case types.MimeType.Mp4:
      return '.mp4';

    case types.MimeType.Webm:
      return '.webm';

    case types.MimeType.Pdf:
      return '.pdf';

    case types.MimeType.Png:
      return '.png';

    case types.MimeType.Text:
      return '.txt';

    case types.MimeType.Usdz:
      return '.usdz';

    case types.MimeType.Zip:
      return '.zip';

    case types.MimeType.Webp:
      return '.webp';

    case types.MimeType.Heic:
      return '.heic';

    case types.MimeType.Svg:
      return '.svg';
  }

  const nope = mimeType;
  return nope;
}

exports.getExtensionFromMimeType = getExtensionFromMimeType;
